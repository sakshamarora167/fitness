<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.orangerabbit.fitness">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogIn" />
        <activity android:name=".SignUp" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme" />
        <activity android:name=".fitnessDetail" />
        <activity android:name=".FoodList" />
        <activity
            android:name=".activity_water"
            android:label="@string/title_activity_water"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".pop"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".waterReminder" />
        <activity android:name=".systemClock"
            android:theme="@style/AppTheme.CustomTheme2"/>
        <receiver android:name=".alarmReciever"/>
        <service android:name=".Notifications"
            android:enabled="true"/>
    </application>

</manifest>